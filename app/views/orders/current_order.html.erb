<p style="color: green"><%= notice %></p>

<h1>Warenkorb</h1>

<div id="orders">
  <% @current_order.line_items.each do |line_item| %>
    <%= line_item.article.name %> |
    Anzahl: <%= line_item.count%> |
    Preis: <%= line_item.total %>€
    <br>
  <% end %>
<%= @a %>
  <hr>
  Der Gesamtpreis beträgt sagenumwobene : <%= @current_order.grand_total %>€
</div>
