<p style="color: green"><%= notice %></p>

<%= render @order %>

  <% @order.line_items.each do |line_item| %>
    Artikel: <%= line_item.article.name %> ||
    Anzahl: <%= line_item.count%> ||
    Preis: <%= format_money(line_item.article.price * line_item.count) %>
    <br>
  <% end %>
  <hr>

  <div>
  <%= link_to "Zurück zu offenen Bestellungen", orders_path %> |
  <%= button_to "Bestellung abgeschlossen", order_finished_path%>
  <%= button_to "Destroy this order", @order, method: :delete %>
</div>
