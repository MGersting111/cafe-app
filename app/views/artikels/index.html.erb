<p style="color: green"><%= notice %></p>

<h1>Artikels</h1>

<div id="artikels">
  <% @artikels.each do |artikel| %>
    <%= render artikel %>
    <p>
      <%= link_to "Show this artikel", artikel %>
    </p>
    <%= link_to "Add", artikels_add_path(artikel)%>
  <% end %>
</div>

<%= link_to "New artikel", new_artikel_path %>




<%= link_to "Show Warenkorb", warenkorbs_path %>
